[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tilemap-tools"
version = "0.1.0"
description = "Outils CLI pour créer et manipuler des tilemaps"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    {name = "Julosse", email = "julosse27110@gmail.com"}
]